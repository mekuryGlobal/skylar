<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.skylar">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.ar" />
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Skylar">
        <activity android:name=".OpportunityActivity"></activity>
        <activity android:name=".learn.CourseDetailActivity" />
        <activity android:name=".learn.CourseActivity" />
        <activity android:name=".learn.AugmentedRealityActivity" />
        <activity android:name=".learn.QuizActivity" />
        <activity android:name=".earn.SendActivity" />
        <activity android:name=".earn.CreateWalletActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".earn.AccountActivity" />
        <activity android:name=".earn.CreateAccountActivity" />
        <activity android:name=".earn.EarnActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".SignUpActivity" />

        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />
    </application>

</manifest>